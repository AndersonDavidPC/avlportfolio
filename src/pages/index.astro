---
import Layout from '../layouts/Layout.astro';
---

<Layout>
	<main class="min-h-screen gradient-bg flex flex-col items-center justify-center relative overflow-hidden">
		<!-- Background decorative elements -->
		<div class="absolute inset-0 overflow-hidden pointer-events-none">
			<div class="absolute -top-40 -right-40 w-80 h-80 bg-primary-200 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-bounce-slow"></div>
			<div class="absolute -bottom-40 -left-40 w-80 h-80 bg-accent-200 rounded-full mix-blend-multiply filter blur-xl opacity-70 animate-bounce-slow" style="animation-delay: 1s;"></div>
		</div>
		
		<div class="card p-12 max-w-2xl mx-4 transform hover:scale-105 transition-all duration-500 relative z-10">
			<div class="mb-8">
				<div class="animate-bounce-slow text-8xl mb-6">🏗️</div>
				<div class="w-32 h-2 bg-gradient-to-r from-primary-500 to-accent-500 mx-auto rounded-full shadow-lg"></div>
			</div>
			
			<h1 class="text-6xl font-bold text-gradient mb-8 font-display">
				AVL Asesorías
			</h1>
			
			<p class="text-xl text-gray-700 mb-8 leading-relaxed">
				Nuestro sitio web está en construcción y estará disponible en los próximos días.
				<br />
				<span class="text-primary-600 font-semibold">¡Gracias por tu paciencia!</span>
			</p>
			
			<div class="flex justify-center space-x-6 mb-8">
				<div class="w-4 h-4 bg-primary-500 rounded-full animate-pulse"></div>
				<div class="w-4 h-4 bg-accent-500 rounded-full animate-pulse" style="animation-delay: 0.2s;"></div>
				<div class="w-4 h-4 bg-primary-600 rounded-full animate-pulse" style="animation-delay: 0.4s;"></div>
			</div>
			
			<div class="text-sm text-gray-500">
				Próximamente: Servicios profesionales de asesoría
			</div>
		</div>
	</main>
</Layout>
